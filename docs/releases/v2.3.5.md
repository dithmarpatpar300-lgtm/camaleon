# Camaleon v2.3.5 — SVG → PNG (Tier 3.3)

**Date:** 2026-06-11  
**Tag:** `v2.3.5`  
**Wave:** Tier 3.3 — SVG rasterize (spike 3.3.0 + tool 3.3.1)

## Summary

**2.3.5** ships **SVG → PNG**: local vector rasterization via `transmutador_svg` (resvg/usvg), output scale presets, PNG compression, prepare probe, alpha-aware PNG export, and Notice Rail `expensive` profile. **20 active tools.**

## User-facing

- New tool **SVG → PNG** (`.svg`, `.svgz`)
- **Output size** presets: 100%, 200%, 512 / 1024 / 2048 px (longest edge, aspect locked)
- Intrinsic vector size + computed output dimensions in the workspace
- Honesty notices: vector→raster, fonts, filters
- 40 MP output cap with scale controls when blocked
- Settings S2 PNG compression default applies

## Technical

- Crate: `motor_transmutacion/transmutador_svg` (Wasm ~1.63 MB, spike GO)
- Worker lazy-load + estimate/transmute routes
- `inspect_svg_meta` in prepare pipeline
- Limit pipeline uses **output** W×H (`MAX_PIXELS`)
- Spike doc: `docs/planning/tier3_3_svg_spike_results.md`

## Versioning

- App **2.3.5** · Engine **1.6.0** (unchanged)
