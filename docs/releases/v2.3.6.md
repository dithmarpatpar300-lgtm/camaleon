# Camaleon v2.3.6 — SVG → JPEG (Tier 3.3.2)

**Date:** 2026-06-11  
**Tag:** `v2.3.6`  
**Wave:** Tier 3.3.2 — SVG rasterize to JPEG + limit-unlock guidance UI

## Summary

**2.3.6** ships **SVG → JPEG**: local vector rasterization with quality + background flattening, semantic alpha probe at prepare, and **LimitUnlockHint** on all limit blockers (pixel, byte, resize) pointing to future Advanced / Risk settings. **21 active tools.**

## User-facing

- New tool **SVG → JPEG** (`.svg`, `.svgz`)
- Same **output size** presets as SVG → PNG (100%, 200%, 512 / 1024 / 2048 px)
- JPEG quality + background color (Settings S2 defaults apply)
- Honesty notices: vector→raster, lossy JPEG, fonts, filters
- Limit blockers include steps to unlock via Settings → Advanced / Risk (feature planned — see `docs/planning/risk_mode_analysis.md`)

## Technical

- Rust: `transmutar_svg_a_jpg_with_options`, `estimate_svg_to_jpg_size`, `assess_svg_meaningful_alpha` (512 px probe)
- Worker JPG routes for `transmutador_svg`
- `LimitUnlockHint` component in `DimensionsBlockPanel`, `OversizeConsentPanel`, `AstroResizePanel`, hard-limit error
- Architecture study: `docs/planning/risk_mode_analysis.md`

## Versioning

- App **2.3.6** · Engine **1.6.0** (unchanged)
